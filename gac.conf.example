# GAC (Git AI Commit) Configuration File
# Copy this file to ~/.config/gac.conf and customize as needed

# ============================================================================
# AI Model Configuration
# ============================================================================

# Option 1: Use Claude CLI (recommended if you have claude CLI installed)
# Uncomment and set these if using Claude CLI
# USE_CLAUDE_CLI=true
# ANTHROPIC_AUTH_TOKEN="your-api-key-here"
# ANTHROPIC_BASE_URL="https://open.bigmodel.cn/api/anthropic"  # or your custom endpoint
# ANTHROPIC_MODEL="GLM-4-Plus"  # or any model you prefer

# Option 2: Use Direct API Call (OpenAI-compatible endpoints)
# Uncomment and set these if using direct API calls
# AI_API_URL="https://api.openai.com/v1/chat/completions"
# AI_API_KEY="your-api-key-here"
# AI_MODEL="gpt-4"

# Example for Yunwu AI (OpenAI compatible)
# AI_API_URL="https://yunwu.ai/v1/chat/completions"
# AI_API_KEY="sk-..."
# AI_MODEL="gpt-4o-mini"

# ============================================================================
# Commit Message Settings
# ============================================================================

# Language for commit messages: "en" or "zh"
LANGUAGE="zh"

# Commit message format: "conventional" or "simple"
# conventional: Uses Conventional Commits format (feat:, fix:, etc.)
# simple: Just a simple one-line message
COMMIT_FORMAT="conventional"

# Maximum number of diff lines to send to AI
# If diff exceeds this, only file list and stats will be sent
MAX_DIFF_LINES=500

# ============================================================================
# Editor Settings
# ============================================================================

# Editor to use when user chooses to edit the commit message
# Defaults to $EDITOR environment variable, or nano if not set
# EDITOR="vim"
# EDITOR="code --wait"
